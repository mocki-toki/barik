Create a PR against the `main` branch of our fork (`bottlebrushes/barik-but-better`), then immediately merge it.

Steps:
1. Push the current branch to `origin` if not already pushed.
2. Create a PR with `gh pr create --repo bottlebrushes/barik-but-better --base main` with a concise title and summary body.
3. Merge the PR with `gh pr merge --repo bottlebrushes/barik-but-better --merge`.
4. Return the merged PR URL.

Do NOT create PRs against the upstream repo (mocki-toki/barik).
